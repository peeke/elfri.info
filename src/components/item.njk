<figure class="item" data-module="Item">
  <div class="item-image">
    <img src="/statics/{{ item.src }}" alt="{{ item.title or 'Geen titel' }}" data-module="Image">
    <div class="item-image-shadow" data-module="Shadow"></div>
  </div>
  <figcaption>
    <h2 class="item-title">{{ item.title or 'Geen titel' }}</h2>
    {% if item.material %}
    <div class="item-description">
      {{ item.material }} {% if item.size %}
      <span class="item-size">{{ item.size }}</span>
      {% endif %}
    </div>
    {% endif %}
  </figcaption>
</figure>